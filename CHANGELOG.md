## 0.1.0 -- 2025-11-25
- Added version functionality (using VERSION and CHANGELOG.md files)
- Normalised `<appInfo>` in all tools; added version
- Replaced `@color` with `@head.color` on `<note>` in MEI output